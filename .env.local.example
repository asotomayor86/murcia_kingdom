# Copia este archivo a .env.local y rellena los valores.

# Cadena de conexión a Neon (Postgres). En Vercel se inyecta automáticamente al
# crear la base de datos desde Storage. En local cópiala desde el panel de Neon.
DATABASE_URL=postgresql://usuario:password@host.neon.tech/dbname?sslmode=require

# Contraseña de invitación compartida. Quien la conozca puede entrar al juego.
# Si la dejas vacía o no la defines, NO habrá barrera de acceso (útil en local).
ACCESO_PASSWORD=

# Pon "1" para habilitar el modo online en el menú (botones crear/unirse sala).
NEXT_PUBLIC_ONLINE=1
